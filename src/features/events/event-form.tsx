"use client";
import { useActionState } from "react";
import { createEvent } from "./actions";
export function EventForm() { const [state, action, pending] = useActionState(createEvent, undefined); return <form action={action} className="card grid gap-5 p-5 sm:grid-cols-2 sm:p-7"><div><label className="label" htmlFor="title">Título</label><input className="input" id="title" name="title" defaultValue="EBD" required/></div><div><label className="label" htmlFor="event_date">Data</label><input className="input" id="event_date" name="event_date" type="date" required/></div><div><label className="label" htmlFor="start_time">Horário</label><input className="input" id="start_time" name="start_time" type="time" defaultValue="09:00"/></div><div className="sm:col-span-2"><label className="label" htmlFor="description">Descrição (opcional)</label><textarea className="input min-h-24" id="description" name="description" maxLength={1000}/></div>{state?.error && <p role="alert" className="text-red-700 sm:col-span-2">{state.error}</p>}<div className="sm:col-span-2"><button className="button-primary" disabled={pending}>{pending ? "Criando…" : "Criar EBD"}</button></div></form>; }
